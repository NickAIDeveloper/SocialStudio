'use client';

// Phase 5 replaces this stub with side-by-side KPIs. Keeping it minimal so
// Phase 4 can ship the tab shell.
export function CompareSection() {
  return (
    <div className="rounded-xl border border-zinc-800/60 bg-zinc-900/30 p-10 text-center text-sm text-white">
      Compare view coming soon.
    </div>
  );
}
